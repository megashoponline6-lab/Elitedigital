<% var title = 'Configurar Admin'; %>

<div class="d-flex justify-content-between align-items-center mb-3">
  <h2 class="fw-bold text-danger">Configuración Inicial del Administrador</h2>
</div>

<!-- 🔔 Mensajes de error -->
<% if (errores && errores.length) { %>
  <div class="alert alert-danger">
    <% errores.forEach(e => { %>
      <div><%= e.msg %></div>
    <% }) %>
  </div>
<% } %>

<div class="card bg-black border border-secondary rounded-4 shadow-lg p-4 mt-3">
  <form method="post">
    <input type="hidden" name="_csrf" value="<%= csrfToken %>">
    <div class="mb-3">
      <label class="form-label text-light">Usuario</label>
      <input name="usuario" class="form-control bg-dark text-light border-secondary" placeholder="Correo o nombre de usuario" required>
    </div>

    <div class="mb-3">
      <label class="form-label text-light">Contraseña (mínimo 8 caracteres)</label>
      <input type="password" name="password" minlength="8" class="form-control bg-dark text-light border-secondary" placeholder="********" required>
    </div>

    <button class="btn btn-danger w-100 mt-2 py-2">Guardar configuración</button>
  </form>
</div>

<p class="text-secondary small mt-3 text-center">
  ⚙️ Solo se requiere ejecutar esta configuración una vez.
</p>
