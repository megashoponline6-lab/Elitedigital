<% var title = 'Panel Admin'; %>

<div class="d-flex justify-content-between align-items-center mb-3">
  <h2>Panel de Administración</h2>
  <div>
    <a href="/admin/cambiar-password" class="btn btn-warning me-2">Cambiar contraseña</a>
    <a href="/admin/salir" class="btn btn-outline-light">Salir</a>
  </div>
</div>

<div class="row g-4 mt-1">

  <!-- 🧾 RESUMEN GENERAL (MongoDB) -->
  <div class="col-lg-6">
    <div class="card bg-black border-secondary rounded-4 shadow">
      <div class="card-body">
        <h5 class="mb-3">Resumen General</h5>
        <div class="row text-center">
          <div class="col">
            <div class="h4"><%= stats ? stats.totalUsuarios : 0 %></div>
            <div class="text-secondary small">Usuarios totales</div>
          </div>
          <div class="col">
            <div class="h4 text-success"><%= stats ? stats.activos : 0 %></div>
            <div class="text-secondary small">Activos</div>
          </div>
          <div class="col">
            <div class="h4 text-danger"><%= stats ? stats.inactivos : 0 %></div>
            <div class="text-secondary small">Inactivos</div>
          </div>
          <div class="col">
            <div class="h4">$<%= stats ? stats.totalSaldo : 0 %></div>
            <div class="text-secondary small">Saldo total</div>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- 💰 RECARGAR SALDO -->
  <div class="col-lg-6">
    <div class="card bg-black border-secondary rounded-4 shadow">
      <div class="card-body">
        <h5 class="mb-3">Recargar saldo</h5>
        <form class="row g-2" method="POST" action="/admin/recargar">
          <input type="hidden" name="_csrf" value="<%= csrfToken %>">
          <div class="col-md-4">
            <input class="form-control" name="correo" type="email" placeholder="Correo del usuario" required>
          </div>
          <div class="col-md-3">
            <input class="form-control" name="monto" type="number" min="1" placeholder="Monto" required>
          </div>
          <div class="col-md-3">
            <input class="form-control" name="nota" placeholder="Nota (opcional)">
          </div>
          <div class="col-md-2">
            <button class="btn btn-danger w-100">Cargar</button>
          </div>
        </form>
      </div>
    </div>
  </div>

  <!-- 👥 USUARIOS -->
  <div class="col-lg-12">
    <div class="card bg-black border-secondary rounded-4 shadow">
      <div class="card-body">
        <div class="d-flex justify-content-between align-items-center mb-2">
          <h5>Usuarios registrados (MongoDB)</h5>
          <button class="btn btn-sm btn-outline-light" type="button" id="toggleUsuarios">Mostrar todos</button>
        </div>

        <div id="usuariosContainer" style="max-height:250px; overflow-y:hidden; transition:all 0.3s ease;">
          <table class="table table-dark table-sm align-middle mb-0">
            <thead>
              <tr>
                <th>#</th>
                <th>Nombre</th>
                <th>Correo</th>
                <th>País</th>
                <th>Saldo</th>
                <th>Última conexión</th>
                <th>Registro</th>
              </tr>
            </thead>
            <tbody>
              <% if (usuarios && usuarios.length > 0) { %>
                <% usuarios.forEach((u, index) => { %>
                  <tr class="<%= index >= 5 ? 'extra-usuario d-none' : '' %>">
                    <td><%= index + 1 %></td>
                    <td><%= u.nombre %> <%= u.apellido %></td>
                    <td><%= u.correo %></td>
                    <td><%= u.pais || '-' %></td>
                    <td>$<%= u.saldo || 0 %></td>
                    <td>
                      <% if (u.last_login) { %>
                        <%= dayjs(u.last_login).format('DD/MM/YYYY HH:mm') %>
                      <% } else { %>
                        <span class="text-secondary">Nunca</span>
                      <% } %>
                    </td>
                    <td><%= dayjs(u.created_at).format('DD/MM/YYYY') %></td>
                  </tr>
                <% }) %>
              <% } else { %>
                <tr><td colspan="7" class="text-center text-secondary">Sin usuarios registrados</td></tr>
              <% } %>
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </div>

  <!-- 🎬 PLATAFORMAS DE STREAMING -->
  <div class="col-lg-12">
    <div class="card bg-black border-secondary rounded-4 shadow">
      <div class="card-body">
        <h5>Plataformas de Streaming</h5>

        <!-- ➕ Añadir nueva plataforma -->
        <form class="row g-2 mb-3" method="POST" action="/admin/plataforma" enctype="multipart/form-data">
          <input type="hidden" name="_csrf" value="<%= csrfToken %>">
          <div class="col-md-8">
            <input class="form-control" name="nombre" placeholder="Nombre del servicio (ej. Netflix, Spotify...)" required>
          </div>
          <div class="col-md-3">
            <input class="form-control" type="file" name="logoimg" accept="image/*" required>
          </div>
          <div class="col-md-1">
            <button class="btn btn-danger w-100">Añadir</button>
          </div>
        </form>

        <!-- 🧾 Tabla de plataformas -->
        <table class="table table-dark table-sm align-middle">
          <thead>
            <tr>
              <th>Logo</th>
              <th>Nombre</th>
              <th>Disponible</th>
              <th>Cambiar logo</th>
              <th>Acción</th>
            </tr>
          </thead>
          <tbody>
            <% if (productos && productos.length > 0) { %>
              <% productos.forEach(p => { %>
                <tr>
                  <td>
                    <% if (p.logo) { %>
                      <img src="<%= p.logo %>" style="height:40px; border-radius:8px">
                    <% } else { %>
                      <span class="text-secondary">Sin logo</span>
                    <% } %>
                  </td>
                  <td class="fw-bold"><%= p.nombre %></td>
                  <td>
                    <% if (p.disponible) { %>
                      <span class="text-success fw-bold">Sí</span>
                    <% } else { %>
                      <span class="text-danger fw-bold">No</span>
                    <% } %>
                  </td>
                  <td>
                    <form method="POST" action="/admin/plataforma/<%= p.id %>/logo" enctype="multipart/form-data" class="d-flex align-items-center gap-2">
                      <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                      <input type="file" name="logoimg" accept="image/*" class="form-control form-control-sm bg-dark text-light" required>
                      <button class="btn btn-sm btn-outline-warning">Actualizar</button>
                    </form>
                  </td>
                  <td>
                    <form method="POST" action="/admin/plataforma/<%= p.id %>/eliminar" onsubmit="return confirm('¿Eliminar esta plataforma?')">
                      <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                      <button class="btn btn-sm btn-outline-danger">Eliminar</button>
                    </form>
                  </td>
                </tr>
              <% }) %>
            <% } else { %>
              <tr><td colspan="5" class="text-center text-secondary">No hay plataformas registradas</td></tr>
            <% } %>
          </tbody>
        </table>
      </div>
    </div>
  </div>

  <!-- 📋 GESTIÓN DE CUENTAS -->
  <div class="col-lg-12">
    <div class="card bg-black border-secondary rounded-4 shadow">
      <div class="card-body d-flex justify-content-between align-items-center">
        <div>
          <h5 class="mb-1">📋 Gestión de Cuentas</h5>
          <p class="text-secondary mb-1">
            Administra las cuentas de cada plataforma (correos, contraseñas y cupos disponibles).
          </p>
          <span class="badge bg-danger fs-6 mt-1">
            <%= stats && stats.totalAccounts ? stats.totalAccounts : 0 %> cuentas registradas
          </span>
        </div>
        <a href="/admin/cuentas" class="btn btn-danger btn-lg px-4">
          Ir a la gestión
        </a>
      </div>
    </div>
  </div>

</div>

<script>
  const btnToggle = document.getElementById('toggleUsuarios');
  const container = document.getElementById('usuariosContainer');
  const extras = document.querySelectorAll('.extra-usuario');
  let abierto = false;

  btnToggle.addEventListener('click', () => {
    abierto = !abierto;
    extras.forEach(row => row.classList.toggle('d-none', !abierto));
    container.style.maxHeight = abierto ? '600px' : '250px';
    container.style.overflowY = abierto ? 'auto' : 'hidden';
    btnToggle.textContent = abierto ? 'Ocultar' : 'Mostrar todos';
    if (!abierto) container.scrollTo({ top: 0, behavior: 'smooth' });
  });
</script>
