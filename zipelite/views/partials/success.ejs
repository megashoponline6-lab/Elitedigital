<% if (typeof ok !== 'undefined' && ok) { %>
  <div class="alert alert-success border-0 rounded-4 shadow-sm py-2 px-3 mt-2 d-flex align-items-center">
    <i class="bi bi-check-circle-fill me-2"></i>
    <span><%= ok %></span>
  </div>
<% } %>
